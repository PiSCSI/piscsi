# PiSCSI creates piscsi0 dynamically while a DaynaPort is attached. NetworkManager
# must not assign an address or alter its link state; piscsi-proxyarp.service owns it.
[keyfile]
unmanaged-devices=interface-name:piscsi0
